{
	"name": "germania-kg/pathserviceprovider",

	"license": "proprietary",
	"authors": [
		{
			"name": "Carsten Witt",
			"email": "carstenwitt@germania-kg.de",
			"role": "Developer"
		}
	],
	"autoload": {
		"psr-4": {
			"Germania\\PathServiceProvider\\": "src/"
		}
	},

	"autoload-dev": {
	    "psr-4": { "tests\\": "tests/src" }
	},

	"require": {
		"php": "^5.6|^7.0",
		"pimple/pimple": "^3.0"
	},

    "require-dev": {
        "php-coveralls/php-coveralls": "^2.0",
        "phpunit/phpunit": "^5.7|^6.0|^7.0"
    },

    "scripts": {
          "phpunit": "phpunit",

          "test": [
              "@phpunit"
          ]
    },

	"suggest": {
		"germania-kg/pathprefixer": "Callable that prefixes a paths array (or StdClass) with a base directory"
	}
}
